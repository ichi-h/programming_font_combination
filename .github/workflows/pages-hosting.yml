name: Deploy to GitHub Pages
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - run: |
          git config --global user.name "ippee"
          git config --global user.email "ippee.music@gmail.com"
          git remote set-url origin https://github.com/ippee/programming_font_combination.git
          yarn && yarn deploy
